# Background image default URL, used by Thymeleaf templates when no zone-specific
# image has been uploaded. Override per environment via BACKGROUND_IMAGE_DEFAULT_URL
# in k8s-deploy values.yaml.
background-image.default-url=${BACKGROUND_IMAGE_DEFAULT_URL:/resources/predix/images/uaa-test.jpg}
